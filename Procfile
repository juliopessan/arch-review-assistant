# Railway / Render / any PaaS deploy
# Set these environment variables in your platform dashboard:
#
#   ANTHROPIC_API_KEY=sk-ant-...   (if using Claude)
#   OPENAI_API_KEY=sk-...          (if using OpenAI)
#   GEMINI_API_KEY=...             (if using Gemini)
#   MISTRAL_API_KEY=...            (if using Mistral)

web: streamlit run web/app.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true
